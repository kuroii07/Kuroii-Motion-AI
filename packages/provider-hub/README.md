# @kuroii/provider-hub

Provider Hub 是模型连接中心、平台接入向导、模型发现中心、任务绑定中心和健康监控中心。

Phase 0 包含 Provider Manifest、Adapter 契约和 Provider Error Recovery Guidance。真实 `testConnection`、`listModels` 和 `execute` 在后续阶段实现。
